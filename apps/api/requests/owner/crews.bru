meta { name: Cuadrillas — listar
  type: http
  seq: 14
}
get { url: {{base_url}}/crews
  body: none
  auth: none
}
script:post-response {
  if (res.body[0]) bru.setVar("crew_id", res.body[0].id);
}
assert { res.status: eq 200 }
