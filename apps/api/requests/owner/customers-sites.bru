meta { name: Clientes — sitios
  type: http
  seq: 3
}
get { url: {{base_url}}/customers/{{customer_id}}/sites
  body: none
  auth: none
}
script:post-response {
  if (res.body[0]) bru.setVar("site_id", res.body[0].id);
}
assert { res.status: eq 200 }
