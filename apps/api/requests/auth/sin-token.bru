meta {
  name: Endpoint protegido sin token
  type: http
  seq: 5
}

get {
  url: {{base_url}}/projects
  body: none
  auth: none
}

assert {
  res.status: eq 401
  res.body.message: eq Falta el token
}
