meta {
  name: Refresh
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "refreshToken": "{{refresh_token}}"
  }
}

assert {
  res.status: eq 200
  res.body.accessToken: isString
}
