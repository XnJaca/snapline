// Copiar a `Secrets.xcconfig` —que no se versiona— y poner la llave real.
//
// Sin llave la app compila y corre: el mapa queda gris y todo lo demás anda.
// La llave de iOS es distinta de la de Android: cada una se restringe a su
// plataforma, así que una filtrada no sirve en la otra.
MAPS_API_KEY =
